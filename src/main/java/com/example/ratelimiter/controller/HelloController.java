package com.example.ratelimiter.controller;

// HelloController is intentionally disabled.
// /hello is served by BackendServerConfig on port 8081
// and rate-limited by the Spring Cloud Gateway on port 8080.

